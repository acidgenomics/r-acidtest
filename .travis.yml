language: general

services:
  - docker

branches:
  only:
    - master

before_install:
  - chmod +x travis-docker.sh

jobs:
  include:
    - stage: test
      name: "basejump"
      script: ./travis-docker.sh

after_failure:
  - env | sort
  - df -h

notifications:
  email:
    recipients:
      - mike@steinbaugh.com
    on_success: change
    on_failure: change
