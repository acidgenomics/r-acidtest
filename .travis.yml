language: general

services:
  - docker

before_install:
  - env | sort

jobs:
  include:
    - stage: test
      name: "basejump-latest"
      script:
        - chmod +x travis-docker.sh
        - ./travis-docker.sh

after_failure:
  - df -h

notifications:
  email:
    recipients:
      - mike@steinbaugh.com
    on_success: change
    on_failure: change
