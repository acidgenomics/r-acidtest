language: general

services:
  - docker

before_install:
  - docker pull acidgenomics/bioc-release
  - docker images

script:
  - docker run -ti --user bioc -v $PWD:/acidtest acidgenomics/bioc-release Rscript -e 'source("/acidtest/travis-docker.R")'

notifications:
  email:
    recipients:
      - mike@steinbaugh.com
    on_success: change
    on_failure: change
